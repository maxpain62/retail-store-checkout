apiVersion: apps/v1
kind: Deployment
metadata:
  name: retail-store-checkout-deployment
  labels:
    app: retail-store-checkout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: retail-store-checkout
  template:
    metadata:
      labels:
        app: retail-store-checkout
    spec:
      containers:
        - name: retail-store-checkout-container
          image: 134448505602.dkr.ecr.ap-south-1.amazonaws.com/retail-store-checkout:latest
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 5     
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 5          
          ports:
            - containerPort: 8080
              protocol: TCP
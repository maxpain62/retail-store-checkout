apiVersion: v1
kind: Service
metadata:
  name: retail-store-checkout-service
  labels:
    app: retail-store-checkout
spec:
  selector:
    app: retail-store-checkout
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort